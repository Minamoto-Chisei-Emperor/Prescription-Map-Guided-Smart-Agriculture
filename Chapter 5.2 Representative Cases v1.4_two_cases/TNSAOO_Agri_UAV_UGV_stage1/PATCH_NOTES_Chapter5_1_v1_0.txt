Chapter 5.1 Experiment v1.0

Purpose:
Generate two separate figures for Section 5.1 of the paper:
1) real field boundary only;
2) synthetic prescription scenario.

Main changes:
- added irregular prescription-patch generation via createIrregularZone.m;
- generateEnvironment.m now uses irregular patches by default;
- added plotFieldBoundaryOnly.m;
- added saveFigureFiles.m;
- added runChapter51Experiment.m;
- plotEnvironment.m legend and labels updated for prescription-scene presentation.

Important note:
The first panel mentioned in the manuscript (original satellite image) is still external data and is NOT generated by this MATLAB package.
Only the second and third panels are generated here.
